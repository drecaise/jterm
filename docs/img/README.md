# Manual images

Screenshots for the user manual live here as **PNG** files. The expected filenames and what
each should show are listed in [`../SCREENSHOTS.md`](../SCREENSHOTS.md).

Until a screenshot is added, pages that reference it will show a broken-image icon — that is
expected. Drop the correctly named PNG into this folder and rebuild; no page edits are needed.

This file is excluded from the published site.
